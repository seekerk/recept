- в таблице состава рецептур сделать перестановку строк с использованием мышки (убрать множественное выделение)
- сделать микробиологию (хранение в бд, выбор записи и оформление)
- добавление значений по умолчанию (напр. цвет, консистенция и т.п.)
- перестановка и сортировка рецептур и компонент
- печать
- обработка изменений настроек
- возможность отмены изменений в форме редактирования рецептуры
- возможность отката
- возможность расчетов в ячейке (формулы эксель или внешний калькулятор)
- добавить панельку вставки тегов
	- убрать автозамену
- ! после обновления списка сырья он смещается вверх
- обработка ошибок в таблицах (DataError)

Общее:
  - Проверять наличие БД. Если ее нет, то выдавать форму настроек с предложением указать местоположение. + сделать работу без БД
  - при открытии делать блокировку от изменения.
  - возможность открытия файлов в режиме "только для чтения"
  - добавить пункт меню "последние открытые файлы" (4 шт)
  - панель обзора:
	- удалить книгу из перечня, свойства сделать доступными через меню
	- свойства рецептуры сделать в виде дерева, убрать панель внешнего вида

Предварительный просмотр:
  - экспорт в MSOffice
  - сделать печать из предварительного просмотра + настройки принтера
  - если нет данных, то не выводить пункты (список гостов, список сырья если все не выводятся)

Загрузка/Импорт
  - (доделать импорт сырья для рецептуры 1.0)
  - Обработка нескольких вариантов (если есть дубликаты в БД)
  - более гибкий поиск похожих записей
  
Редактирование справочников:
  - окно фильтрации списка (контекстный поиск + сброс фильтра)
  - при работе открывать только один справочник
  - ввести обозначение заполненых записей (если большинство полей заполнено или выставлен флаг)
  - сделать отмену изменений более мягкой + обновление более мягким
  - выводить предупреждение при попытке сохранить форму с неправильно введеными полями
  - статус измененной БД (сейчас нет привязки)

Версия 0.10
===========

Версия 0.9
==========
Конфигурация:
+ диалог выбора местоположения программы pandoc
+ настройки формирования отчета ТК и ТТК (отображать оригинал, перечень выходов, вода) + загрузка и сохранение

Отчеты:
! вызов программы pandoc 1.2.1
! правки шаблона ТТК
+ отчет Технологическая карта (ТК)
+ добавлена возможность изменения настроек после формирования отчета
! влажность если не задана, то расчитывается

Разное:
+ поле "компания разработчика" в свойствах книги + интерфейс
! поле "ФИО и Должность начальника" разделено на "ФИО начальника" и "Должность начальника"
+ явное разделение выбора метода расчета: переключатели для выбора методики расчета 
	(по воде, по выходу или с расчетом воды)
+ поле "выход в шт" (countExit) для обозначения выхода в шт.
! объединение описательных полей в одну обработку, единый список для них, PanelReceptProcess больше не используется
! Корректная обработка создания новой книги: пользователю дается возможность сохранить текущие изменения
! исправлена ошибка потери фокуса в списке рецептур
+ кислотность изделия (рецептура)

Версия 0.8
==========
+ поддержка базы данных в формате SQLite
+ загрузка/сохранение настроек в файл
! добавление в настройки используемого типа БД
+ при импорте компонент подсветка измененных записей и выбор элемента из БД
! поправлена работа с таблицей рецептуры (выпадение списка названий при нажатии)
+ добавлен скелет фильтрованного комбобокса (ComboBoxFilter)
! округление до первой значащей цифры
+ сортировка рецептур в книге
! убрана общая форма книги рецептур (надо сделать ее в качестве свойства).
+ отчет "перечень ТТК"

Версия 0.7
==========
+ работа с вложенными рецептурами
! перенесен массив вложенных объектов в базовую структуру, оптимизировано дерево
! Структуры данных: проверка изменения при присвоении (если действительно изменен, то вызывается событие)
! поправлено удаление для вложенных рецептур
+ переезд на UTF-8
! адаптация к Mono
! убраны дефолтные значения полей (чтобы не мешались)
! небольшие фиксы падений при изменении данных
+ добавлен расчет процента заполнения элемента (число заполненных полей к общему числу полей)
! переезд с OLE DB на ODBC

Версия 0.6
==========
  ! сохранение рецептур если не все введено
  ! обновление базы данных (Кнопка update) теперь выполняет запрос к БД
Форма показатели:
  + начальный вариант ввода микробиологии
Предварительный просмотр:
  ! при экспорте название файла = название рецептуры
  ! при расчете ТТК учитываются только заполненные записи (есть название и количество :))
  ! Если нельзя сделать экспорт в rtf, то отключаем его
Форма рецептур:
  + брутто и нетто теперь раздельно для каждой рецептуры
  ! Для вычисления брутто/нетто используется точность из конфигурации
Редактирование справочников:
  ! ошибка, возникающая при сохранении компоненты (выход за границы списка)
  ! работа с отрицательными значениями потерь при редактировании
  ! более длинное поле название для компонент и потерь обработки
  ! переделана выборка под структуру DataBaseType
  ! Ошибка: если нажать "добавить запись" или "копировать запись", то изменения предыдущей редактируемой записи пропадают.

Версия 0.5
==========
+ добавить в сырье поля "показывать в рецептуре" и "показывать в составе"
! сохранение не сбрасывает флаг изменения?
Форма настройки:
  +  диалог выбора файла для бд и шаблона, кнопка "сбросить настройки" удаляет записи в реестре пользователя
  ! изменение сохранения настроек (теперь пользовательские главнее)
  ! диалог выбора БД выдает запрос на сохранение. Переделать на просто выбор
Форма рецептур:
  ! пофиксен интерфейс (наезды и метки)
  + в рецептуру добавлены поля "описание" (п. 1.2 ТТК и начало рецептуры), "нормативный документ" и "источник"
Форма редактирования БД:
  + добавить кнопку "обновить данные"
! при открытии списков компонент, потерь по умолчанию открывается первый элемент

Версия 0.4
+ обработка ошибок для последующего исправления (сохранение ошибки и стека)
! ошибка: если сохранить, затем продолжить ввод, а потом снова сохранить, то сохраняется пустая структура
+ добавить изменение текста в зависимости от компоненты или потери
! отсортировать переходы в форме ввода компонент
+ форма настроек рецептур
+ импорт из рецептуры 1.0
+ добавлено поле выхода рецептуры и изменен расчет по выходу если он задан
+ автозамена %grad на символ градуса, минусов на длинные тире

Версия 0.3 (1)
+ поле версии файла книги. Для Recept2 - 1, для Recept - 0. В скобках указывается версия файла
! расчет рецептуры только по натуре.
! БД (comps): поле "углеводы" (carbohydrate) разбито на "крахмал" (starch) и "моно и дисахариды" (saccharides)
+ БД (comps): добавлены поля "органические кислоты" (acid) и "зола" (ash)
! БД: таблица общих потерь (poteri) переименована -> totalLoss
+ БД: добавлена таблица производственных потерь (processLoss). Структура как и у компонент
+ углеводы разделить на "моно и дисахариды" и "крахмал"
+ добавить "органические кислоты" и "зола"
+ добавление, изменение и удаление производственных потерь
+ расчет рецептуры в соответствии с горячей обработкой

Версия 0.2
+ обработка неправильных значений
! поправлена запись для пустой рецептуры
+ расчет данных для ТТК
+ работа с БД сырья и др. данных
	+ форма работы с данными из БД
	+ добавление, изменение и удаление компонент и потерь в БД
+ обработка изменений в бд
+ связь расширения rcp2 с программой
+ возможность отмены изменений в форме работы с БД

Версия 0.1
+ чтение настроек программы из конфига
+ запуск проги с параметром "имя файла"
+ файл-шаблон книги рецептур
+ хранение данных (потери, компоненты) в книге
+ привязка изменения полей структур данных к эвентам
+ установщик (версия, файлы, записи в реестр)

